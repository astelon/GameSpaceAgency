name: Tests

on:
  push:
  workflow_dispatch:

jobs:
  backend:
    name: Backend (PHP engine & card data)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
      - name: Card database integrity (schema + PHP/JSON consistency)
        run: php webGame/tools/test_cards_data.php
      - name: Engine fuzz/smoke games
        run: php webGame/tools/test_engine.php 30
      - name: Scripted rule scenarios
        run: php webGame/tools/test_scenarios.php
      - name: Sub-orbital decay rules
        run: php webGame/tools/test_suborbital.php
      - name: HTTP API integration (auth, hand hiding, storage locking)
        run: php webGame/tools/test_api.php

  parity:
    name: PHP<->JS rules parity
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Generate fixtures from the PHP engine and replay them through data.js
        run: npm run test:parity

  frontend:
    name: Frontend (card rendering + smoke)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium
      - name: Card sizing + hot-seat smoke test
        run: npx playwright test
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-results
          path: test-results/
          retention-days: 7
